name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v0.1.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  # Create draft release
  create-release:
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create-release.outputs.release_id }}
      upload_url: ${{ steps.create-release.outputs.upload_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Get version
        id: get-version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.tag }}" >> $GITHUB_ENV
          else
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          fi

      - name: Create Release
        id: create-release
        uses: actions/github-script@v7
        with:
          script: |
            const { data } = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: process.env.VERSION,
              name: `Fat Clip ${process.env.VERSION}`,
              body: '## What\'s New\n\nSee [CHANGELOG.md](CHANGELOG.md) for details.\n\n## Installation\n\n### Windows\n- Download `.msi` or `.exe` installer\n\n### macOS\n- Download `.dmg` installer\n\n### Linux\n- Download `.AppImage` or `.deb` package',
              draft: true,
              prerelease: false
            })
            core.setOutput('release_id', data.id)
            core.setOutput('upload_url', data.upload_url)

      - name: Debug release metadata
        run: |
          echo "event_name=${{ github.event_name }}"
          echo "ref_name=${{ github.ref_name }}"
          echo "release_id=${{ steps.create-release.outputs.release_id }}"
          echo "upload_url=${{ steps.create-release.outputs.upload_url }}"

  # Build for Windows
  build-windows:
    needs: create-release
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri App
        run: npx tauri build

      - name: Find Windows bundles
        id: find-windows-assets
        shell: bash
        run: |
          MSI_PATH=$(find ./src-tauri/target/release/bundle/msi -maxdepth 1 -name "*.msi" | head -n 1)
          NSIS_PATH=$(find ./src-tauri/target/release/bundle/nsis -maxdepth 1 -name "*.exe" | head -n 1)
          if [ -z "$MSI_PATH" ] || [ -z "$NSIS_PATH" ]; then
            echo "Failed to locate Windows bundle files"
            find ./src-tauri/target/release/bundle -maxdepth 3 -type f
            exit 1
          fi
          echo "msi_path=$MSI_PATH" >> $GITHUB_OUTPUT
          echo "nsis_path=$NSIS_PATH" >> $GITHUB_OUTPUT

      - name: Resolve release tag
        shell: pwsh
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            "RELEASE_TAG=${{ github.event.inputs.tag }}" | Out-File -FilePath $env:GITHUB_ENV -Append
          } else {
            "RELEASE_TAG=${{ github.ref_name }}" | Out-File -FilePath $env:GITHUB_ENV -Append
          }

      - name: Debug Windows upload context
        shell: bash
        run: |
          echo "release_tag=$RELEASE_TAG"
          echo "release_id=${{ needs.create-release.outputs.release_id }}"
          echo "upload_url=${{ needs.create-release.outputs.upload_url }}"
          echo "msi_path=${{ steps.find-windows-assets.outputs.msi_path }}"
          echo "nsis_path=${{ steps.find-windows-assets.outputs.nsis_path }}"
          gh --version
          gh release view "$RELEASE_TAG" --json id,tagName,url,isDraft

      - name: Upload MSI
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: gh release upload "$RELEASE_TAG" "${{ steps.find-windows-assets.outputs.msi_path }}" --clobber

      - name: Upload NSIS
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: gh release upload "$RELEASE_TAG" "${{ steps.find-windows-assets.outputs.nsis_path }}" --clobber

  # Build for macOS
  build-macos:
    needs: create-release
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri App
        run: npx tauri build

      - name: Find macOS bundles
        id: find-macos-assets
        run: echo "APP_VERSION=$(node -p "require('./src-tauri/tauri.conf.json').version")" >> $GITHUB_ENV
        shell: bash
      - name: Resolve macOS asset paths
        id: resolve-macos-assets
        shell: bash
        run: |
          DMG_PATH=$(find ./src-tauri/target/release/bundle/dmg -maxdepth 1 -name "*.dmg" | head -n 1)
          APP_TAR_PATH=$(find ./src-tauri/target/release/bundle/macos -maxdepth 1 -name "*.app.tar.gz" | head -n 1)
          if [ -z "$DMG_PATH" ] || [ -z "$APP_TAR_PATH" ]; then
            echo "Failed to locate macOS bundle files"
            find ./src-tauri/target/release/bundle -maxdepth 3 -type f
            exit 1
          fi
          echo "dmg_path=$DMG_PATH" >> $GITHUB_OUTPUT
          echo "app_tar_path=$APP_TAR_PATH" >> $GITHUB_OUTPUT

      - name: Resolve release tag
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "RELEASE_TAG=${{ github.event.inputs.tag }}" >> $GITHUB_ENV
          else
            echo "RELEASE_TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          fi

      - name: Debug macOS upload context
        shell: bash
        run: |
          echo "release_tag=$RELEASE_TAG"
          echo "release_id=${{ needs.create-release.outputs.release_id }}"
          echo "upload_url=${{ needs.create-release.outputs.upload_url }}"
          echo "dmg_path=${{ steps.resolve-macos-assets.outputs.dmg_path }}"
          echo "app_tar_path=${{ steps.resolve-macos-assets.outputs.app_tar_path }}"
          gh --version
          gh release view "$RELEASE_TAG" --json id,tagName,url,isDraft

      - name: Upload DMG
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "$RELEASE_TAG" "${{ steps.resolve-macos-assets.outputs.dmg_path }}" --clobber

      - name: Upload App
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "$RELEASE_TAG" "${{ steps.resolve-macos-assets.outputs.app_tar_path }}" --clobber

  # Build for Linux
  build-linux:
    needs: create-release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libglib2.0-dev libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
          npm ci

      - name: Build Tauri App
        run: npx tauri build

      - name: Find Linux bundles
        id: find-linux-assets
        shell: bash
        run: |
          APPIMAGE_PATH=$(find ./src-tauri/target/release/bundle/appimage -maxdepth 1 -name "*.AppImage" | head -n 1)
          DEB_PATH=$(find ./src-tauri/target/release/bundle/deb -maxdepth 1 -name "*.deb" | head -n 1)
          if [ -z "$APPIMAGE_PATH" ] || [ -z "$DEB_PATH" ]; then
            echo "Failed to locate Linux bundle files"
            find ./src-tauri/target/release/bundle -maxdepth 3 -type f
            exit 1
          fi
          echo "appimage_path=$APPIMAGE_PATH" >> $GITHUB_OUTPUT
          echo "deb_path=$DEB_PATH" >> $GITHUB_OUTPUT

      - name: Resolve release tag
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "RELEASE_TAG=${{ github.event.inputs.tag }}" >> $GITHUB_ENV
          else
            echo "RELEASE_TAG=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          fi

      - name: Debug Linux upload context
        shell: bash
        run: |
          echo "release_tag=$RELEASE_TAG"
          echo "release_id=${{ needs.create-release.outputs.release_id }}"
          echo "upload_url=${{ needs.create-release.outputs.upload_url }}"
          echo "appimage_path=${{ steps.find-linux-assets.outputs.appimage_path }}"
          echo "deb_path=${{ steps.find-linux-assets.outputs.deb_path }}"
          gh --version
          gh release view "$RELEASE_TAG" --json id,tagName,url,isDraft

      - name: Upload AppImage
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "$RELEASE_TAG" "${{ steps.find-linux-assets.outputs.appimage_path }}" --clobber

      - name: Upload DEB
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "$RELEASE_TAG" "${{ steps.find-linux-assets.outputs.deb_path }}" --clobber

  # Publish release
  publish-release:
    needs: [create-release, build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    steps:
      - name: Publish Release
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: ${{ needs.create-release.outputs.release_id }},
              draft: false
            })
